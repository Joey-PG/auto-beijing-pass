# 已知问题与改进清单

更新时间：2026-08-08

本文记录当前代码、业务规则、ECS 运行方式和发布流程中已确认的问题。
状态含义：

- `处理中`：本轮正在修复；
- `待处理`：问题已确认，尚未修改；
- `已完成`：已修复并通过测试。

## 高优先级

- [x] `已完成` 定时任务改为执行成功后才写入“当天已运行”；失败后每 5 分钟
  重试，并在随机窗口结束后保留 30 分钟补偿检查。cron tick 增加单实例锁，
  业务 API 和通知请求分别增加 20 秒、15 秒超时。
- [x] `已完成` 已有账号重新执行 `init --force` 时，未显式传入的进京证类型
  和通知渠道会保留原配置；新账号及代码兜底类型统一改为“六环外”。
- [ ] `待处理` 项目已更名为 `auto-beijing-pass`、命令已更名为
  `auto-bj-pass`，但新 npm 包尚未发布；正式发布前全局安装文档还不能直接使用。
- [x] `已完成` Web 后台添加账号时会明确选择系统默认或账号自定义出行配置；
  未选择配置的旧账号不会静默提交申请，配置变更审计也会记录本次配置来源。
- [ ] `待处理` 北京通密码在登录成功后仍以明文保存在配置文件中，并且
  `--password` 参数可能进入 shell 历史或进程列表。
- [ ] `待处理` `cron.log` 是未经脱敏的标准输出；非法通知 URL 可能原样写入
  日志。审计日志写入失败会被静默忽略，缺少审计失效告警和防篡改能力。

## 中优先级

- [ ] `待处理` `crontab -l` 的任何失败都会被视为空配置，重新执行
  `cron setup` 时可能覆盖同一用户已有的其他 cron 任务。
- [ ] `待处理` 配置文件和 cron 状态文件采用直接覆盖写入，没有使用原子
  临时文件、文件锁或损坏恢复机制。
- [ ] `待处理` `package.json` 声明支持 Node.js 18，但当前
  `@inquirer/prompts` 实际要求 Node.js 20.12 或更高版本。
- [ ] `待处理` 日期加减函数混用 UTC 解析和本地日期操作，在负时区可能产生
  一天偏差；固定 cron 表达式的触发时区也依赖服务器系统时区。
- [ ] `待处理` 生产依赖审计存在 2 个高危和 19 个中危传递依赖问题；OCR
  处理远端验证码图片时没有限制响应体大小和图片尺寸。
- [ ] `待处理` 提交接口返回成功后直接显示“续签成功”，没有重新查询确认
  新申请状态，更准确的语义应为“申请已提交”。
- [ ] `待处理` `vehicle swap` 先添加新车辆再删除旧车辆，第二步失败时没有
  回滚或补偿，会留下部分完成状态。
- [ ] `待处理` 缺少真实到期提交、失败重试、并发互斥、token 失效、重新登录
  配置保留、通知超时、多条证件记录排序等测试，也没有 CI、lint 和覆盖率门禁。
- [ ] `待处理` 本地大范围改动尚未形成可追溯提交，ECS 目录也没有 Git 元数据；
  当前审计日志中的 `1.0.2` 无法唯一定位实际运行代码。
